{
  "name": "inspector-sipucol",
  "version": "1.0.0",
  "description": "Inspector Sipucol - aplicacion multiplataforma para inspeccion",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite --host 127.0.0.1 --port 5173",
    "backend": "py -m uvicorn backend.server:app --host 127.0.0.1 --port 8000 --no-access-log",
    "dev:full": "concurrently -n BACKEND,FRONTEND -c green,cyan \"py -m uvicorn backend.server:app --host 127.0.0.1 --port 8000 --no-access-log\" \"vite --host 127.0.0.1 --port 5173\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.2",
    "concurrently": "^10.0.3",
    "electron": "^42.3.0",
    "vite": "^8.0.16"
  },
  "dependencies": {
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "xlsx": "^0.18.5"
  },
  "private": true
}
